<td class="message">
  <%= t('.text', 
        :user => character_name_link(app_request.sender), 
        :item => content_tag(:strong, app_request.item.name)
      ).html_safe %>
</td>
<td class="image"><%= item_image(app_request.item, :small) %></td>
