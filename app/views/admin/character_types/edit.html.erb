<%= admin_title(t(".title", :name => @character_type.name)) %>

<% form_for [:admin, @character_type], :html => {:multipart => true} do |form| %>
  <%= form.fields %>

  <%= form.submit_and_continue %>
<% end %>
