<% content_tag :div, :class => payout.name do %>
  <% if payout.property_set %>
    <%= "%s: Random Property from %s (%d%% %s)" % [
          payout.apply_on_label,
          payout.property_set.name,
          payout.chance,
          payout.action
        ] %>
  <% end %>
<% end %>
