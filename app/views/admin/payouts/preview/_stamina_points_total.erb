<% content_tag :div, :class => payout.name do %>
  <%= "%s: %d %s (%d%% %s)" % [
    payout.apply_on_label,
    payout.value,
    Character.human_attribute_name("total_stamina_points"),
    payout.chance,
    payout.action
  ] %>
<% end %>
