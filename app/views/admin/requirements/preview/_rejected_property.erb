<% content_tag :div, :class => requirement.name do %>
  <%= ("#{t("requirements.rejected_property.rejects")} %s %s" % [
      (requirement.property.visible? ? '' : content_tag(:span, admin_state(requirement.property), :class => :state)),
      requirement.property.name,
    ]).html_safe %>
<% end %>
