<%= admin_title(t(".title", :name => @monster_type.name)) %>

<% form_for [:admin, @monster_type], :html => {:multipart => true} do |form| %>
  <%= form.fields %>

  <%= form.submit_and_continue %>
<% end %>
